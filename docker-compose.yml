version : '3'
services:
    web:
       environment:
          - CHOKIDAR_USEPOLLING=true
       build: 
          context: .
          dockerfile: Dockerfile.dev
       ports:
         - "8080:8080"
      #  volumes:
      #    - /app/node_modules
      #    - .:/app 
       volumes:
         - /app/node_modules
         - .:/app